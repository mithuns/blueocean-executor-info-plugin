import React from 'react';
import { Route } from 'react-router';
import { ExecutorInfoPage } from './ExecutorInfoPage';

export default (
    <div>
        <Route path="/executor-info-new" component={ExecutorInfoPage} />
    </div>
);